<%= link_to(video) do %>
  <div class="row">
    <div class="col-sm-4 col-xs-12">
      <img class="img-responsive pull-left" src="<%= video.thumbnail_url %>" alt="" width="200">
    </div>
    <div class="col-sm-7 col-xs-10">
      <h3 style="margin-top:0px"><%= video.title unless video.title.nil? %><br></h3>
      <p class="text-muted">
        <small>
          <%= render partial: 'videolink', locals: {video: video} %>
          &nbsp;
          <%= video.duration unless video.duration.nil? %>
        </small>
      </p>
    </div>
  </div>
<% end %>
